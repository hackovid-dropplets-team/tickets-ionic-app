import { AbstractModelService } from './abstract-model-service';

describe('AbstractModelService', () => {
  it('should create an instance', () => {
    expect(new AbstractModelService()).toBeTruthy();
  });
});
